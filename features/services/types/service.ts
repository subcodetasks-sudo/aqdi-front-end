export type ServiceType = "residential" | "commercial";

export type ServiceItemTranslations = {
  eyebrow: string;
  titleLine1: string;
  titleLine2: string;
  description: string;
  statsValue: string;
  statsText: string;
};
